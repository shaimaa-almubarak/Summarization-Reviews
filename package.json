{
   "name": "my-app",
   "module": "index.ts",
   "type": "module",
   "private": true,
   "scripts": {
      "dev": "bun run index.ts",
      "prepare": "node -e \"const fs = require('fs'); if (fs.existsSync('.git')) { require('child_process').spawnSync('husky', ['install'], { stdio: 'inherit' }); }\""
   },
   "devDependencies": {
      "@types/bun": "latest",
      "concurrently": "^9.2.1",
      "husky": "^9.1.7",
      "lint-staged": "^16.2.7",
      "prettier": "^3.2.5"
   },
   "peerDependencies": {
      "typescript": "^5"
   },
   "engines": {
      "node": "22.x"
   },
   "workspaces": [
      "packages/*"
   ],
   "dependencies": {
      "dotenv": "^17.2.3"
   }
}
